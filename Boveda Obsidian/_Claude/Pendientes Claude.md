---
title: Pendientes Claude
date: 2026-09-04
tags:
  - claude
  - pendientes
  - tareas
status: activo
---

# Pendientes Claude

> [!tip] Cómo usar esta nota
> Al iniciar una sesión, revisar **Bugs activos** y **Alta prioridad**. Al terminar, mover lo completado a **Completados** y agregar nuevos pendientes. Esta lista sale de comparar el código real contra [[Patilandia — Brief Original]] el 2026-09-04.

## Migración Medusa → Vendure (completa — 2026-09-10 a 2026-09-11)

Decisión del usuario: reemplazar Medusa por Vendure como motor ecommerce, manteniendo el storefront Next.js propio y construyendo un "Patilandia Admin" sobre el Dashboard extensible de Vendure. Plan completo (auditoría + 7 fases) en [[Decisiones y Razonamiento]]. **Las 7 fases están completas** — Medusa ya no existe en el proyecto (ver Fase 7 abajo).

- [x] **Fase 1 — Scaffold de Vendure.** Repo hermano `C:\Users\Leonardo\Desktop\patilandia-vendure` (Vendure 3.7.3), Postgres aislado en Docker (`patilandia-vendure-postgres_db-1`, puerto 6543, vía el `docker-compose.yml` propio del proyecto). Server+worker+Dashboard corriendo en `http://localhost:3000`.
- [x] **Fase 2 — Modelado del catálogo real.** Categorías → Collections filtradas por Facet `category`; colecciones temáticas (royal/galaxy/magic/safari/dreams) → Collections filtradas por Facet `theme`; petType → Facet `pet-type`; talla/color → ProductOptionGroups reales con ProductVariants (resuelve la deuda de "variante única" que tenía Medusa); resto de campos (materials, care, highlights, rating, reviewCount, badge, shortDescription, weightKg, shippingClass) → customFields tipados. Seed en `patilandia-vendure/src/scripts/seed-patilandia.ts`, idempotente (limpia sample data + assets en cada corrida). Los 8 productos reales, sus 96 variantes y las 12 colecciones verificados vía Shop API GraphQL.
- [ ] **`synchronize: true` sigue así a propósito (confirmado con el usuario 2026-09-11)** — queda en desarrollo mientras el esquema de `customFields` siga en movimiento; recién antes de un despliegue real hay que apagarlo y generar la migración real con `npx vendure migrate` contra una DB limpia. No bloquea nada en local, no es urgente.
- [ ] **Facet "color" huérfano de la sample data — el usuario lo va a borrar él mismo** (2026-09-11) cuando entre al Dashboard a cargar productos nuevos (Settings → Facets, un par de clicks). Cosmético, cero urgencia, no hace falta que Claude lo toque.
- [ ] **Bug de Vendure en Windows encontrado y parcheado:** `LocalAssetStorageStrategy` arma la URL pública del asset con `path.join` (separador de Windows, `\`), rompiendo cualquier URL de imagen en el browser. Se agregó un `storageStrategyFactory` en `vendure-config.ts` que normaliza `\`→`/`. Vale la pena reportarlo/revisarlo contra versiones futuras de `@vendure/asset-server-plugin`.
- [x] **Fase 3 — Storefront lee de Vendure.** `src/lib/storefront.ts` ahora llama a `vendureFetch`/`adaptVendureProduct` (`src/lib/vendure/client.ts` + `adapters.ts`, nuevos) en vez de `medusaFetch`/`adaptMedusaProduct`. **Ningún componente ni ruta cambió** — se mantuvo la promesa de la capa adaptadora. `next.config.ts` ganó `images.remotePatterns` para el asset server de Vendure (URLs absolutas, a diferencia de los mocks/Medusa que usaban rutas relativas). Antes de migrar se encontraron y corrigieron dos huecos reales que había dejado la Fase 2: `compareAtPrice` y `featured` no se estaban poblando en Vendure (customFields nuevos en `ProductVariant`/`Product`, seed re-corrido). Verificado con `npm run typecheck`, `npm run build` (12/12 páginas, home/tienda prerenderizadas con datos reales de Vendure en build time) y HTTP real en las 9 rutas — nombres, precios, descuentos, colecciones (camitas/accesorios/alimentos) y filtro por categoría todos correctos contra datos reales, no mocks. Esto también cubre lo que pedía la Fase 4 (verificación de paridad) — no se necesitó un paso aparte.
- [x] **Fase 5 — Carrito y checkout reales contra Vendure.** El carrito dejó de ser local/mock — `store/store-provider.tsx` ahora hidrata y muta el `activeOrder` real de Vendure (`src/lib/vendure/shop-client.ts`, nuevo: sesión por bearer token en `localStorage`, `addItemToOrder`/`adjustOrderLine`/`removeOrderLine`/`setCustomerForOrder`/`setOrderShippingAddress`/`setOrderShippingMethod`/`transitionOrderToState`/`addPaymentToOrder`). Pedido explícito del usuario implementado: apenas hay algo en `/carrito`, lo primero que se pide es el correo (`setCustomerForOrder`, guardado contra la orden antes de mostrar los ítems) — confirmado que Vendure no impone ningún orden acá, es 100% decisión de UX nuestra. `/checkout` quedó con dirección de envío, selección real de método de envío (tarifas COP configuradas, ver abajo) y confirmación de pedido real (queda en estado `PaymentAuthorized` vía el `dummy-payment-handler` que trae el scaffold — Wompi/Mercado Pago siguen diferidos, tal como ya estaba decidido). Wishlist se mantiene 100% local (no es un concepto que Vendure modele). Verificado con `npm run typecheck`, `npm run build`, y una simulación completa de la secuencia de mutaciones por `curl` que terminó en una orden real y persistida en Postgres (`PaymentAuthorized`, cliente y total correctos).
- [x] **Fase 6 — Patilandia Admin sobre el Dashboard de Vendure.** Plugin nuevo `patilandia-vendure/src/plugins/patilandia-admin` (scaffoldeado con `npx vendure add -p/-d`, el flujo oficial): logo real de Patilandia en el login (extension point `login.logo`, el único lugar del Dashboard donde vive un logo — la barra lateral autenticada muestra el Channel activo, no un logo, así que no hay nada que reemplazar ahí), texto de bienvenida propio (`login.beforeForm`), paleta de marca completa (violeta/dorado de `globals.css`) vía `theme.light`/`theme.dark` en `vite.config.mts` — verificado que el CSS compilado trae `--primary:#7261ff`, `--sidebar:#fffaf3`, etc. exactos —, y una sección de navegación "Patilandia" nueva con una página placeholder "Mascotas" que prueba el patrón completo plugin → extensión de Dashboard antes de construir las funcionalidades reales (perfiles de mascotas, Patipuntos, reseñas, etc. — cada una futura será su propio plugin siguiendo este mismo molde). Verificado con `npx tsc --noEmit -p tsconfig.dashboard.json` limpio (el único tsconfig que realmente tipa `dashboard/*.tsx`, distinto del `tsconfig.json` del servidor) y con el servidor Vite del Dashboard sirviendo el módulo y el asset del logo sin errores (`Found 1 dashboard extensions`, ambos con 200 real).
- [x] **Idioma de la interfaz del Dashboard, corregido (2026-09-11).** El usuario notó que el Dashboard se veía en inglés (menús "Sellers", "Catalog", "Sales"...) pese a que el canal ya tenía el idioma de contenido en español — son cosas distintas: el idioma de contenido (`Channel.defaultLanguageCode`) controla en qué idioma se editan las traducciones de productos/colecciones, pero el idioma de la propia interfaz del Dashboard (menús, botones) se configura aparte, vía `i18n` en `vendureDashboardPlugin` (`vite.config.mts`). Se agregó `i18n: { defaultLanguage: LanguageCode.es, defaultLocale: 'CO', availableLanguages: [en, es] }` — `@vendure/dashboard` ya trae la traducción `es.po` lista de fábrica, no hubo que traducir nada a mano. Verificado que el virtual module `vendure-ui-config` que lee el Dashboard en runtime ya devuelve `"defaultLanguage":"es"`.
- [ ] **Límite real encontrado, no evitable con más esfuerzo:** el Dashboard de Vendure (a la fecha, 3.7.3) no tiene un extension point soportado para reemplazar/ocultar branding fuera del logo de login — no hay forma de tocar el `<title>`/favicon de la SPA ni ningún "powered by Vendure" desde la API de plugins. Coherente con la estrategia híbrida ya decidida para el admin (ver [[Decisiones y Razonamiento]], 2026-09-04): mientras se use el Dashboard nativo, esto es lo máximo de marca propia que da la plataforma — un "Patilandia Admin" 100% propio seguiría siendo el objetivo de largo plazo si se quiere ir más allá.
- [ ] **Wompi/Mercado Pago** — correctamente diferido, el punto de integración (`addPaymentToOrder`) ya existe y funciona con el handler dummy.
- [x] **Tasa de impuesto corregida al 19% real de Colombia (2026-09-11).** No era la zona "Americas" como se había anotado — el canal tenía `defaultTaxZone` apuntando a "Europe" (20%, coincidencia numérica con "Americas" que hizo parecer que era esa). Se creó una zona "Colombia" real (país CO) con su propia tasa "Standard Tax Colombia" (19%) en `patilandia-vendure/src/scripts/configure-checkout.ts`, y se apuntó el canal a esa zona. Verificado con una orden real vía GraphQL: `taxSummary` devuelve `taxRate: 19`. Ver [[Decisiones y Razonamiento]] para el detalle de por qué `DefaultTaxZoneStrategy` no mira la dirección del pedido para nada.
- [x] **Fase 7 — Baja de Medusa.** Confirmado explícitamente por el usuario, incluyendo borrar `patilandia-backend` del todo (no archivarlo) a pesar de la advertencia de que no tenía commits ni remote — irreversible, decisión informada. Se borró: `src/lib/medusa/` (client.ts, adapters.ts) del storefront, las env vars `NEXT_PUBLIC_MEDUSA_*` de `.env.local`/`.env.example`, el contenedor Docker `patilandia-postgres` (parado y eliminado, sin volumen nombrado que recuperar), y el repo completo `C:\Users\Leonardo\Desktop\patilandia-backend`. Se limpió el copy que todavía mencionaba "Medusa" en 7 archivos (home, categorías, cuenta, footer, layout metadata, product-detail, shipping.ts) — no fue un simple find-replace de la palabra: varias frases estaban en tiempo futuro ("preparado para integrarse con Medusa") y ya no aplican porque la integración real ya existe, así que se reescribieron para reflejar el estado actual. Verificado con `npm run typecheck` y `npm run build` limpios después de borrar los archivos, y ambos servidores (`localhost:3000` Vendure, `localhost:3001` storefront) respondiendo 200 al terminar.
- [x] ~~"Relacionados" en la página de producto usaba los mocks viejos, no Vendure real~~ — resuelto 2026-09-11 al construir la funcionalidad de Recomendaciones (ver el roadmap de funcionalidades propias más abajo y [[Decisiones y Razonamiento]]).
- [ ] **Codegen de GraphQL para el storefront** — hoy `src/lib/vendure/adapters.ts` tipa a mano el shape exacto que pide la query (sin depender del schema completo de Vendure). Es correcto y suficiente para el alcance actual, pero si el catálogo de queries crece vale la pena evaluar `graphql-codegen`/`gql.tada` como se dejó anotado en el plan de migración.

## Bugs activos

*(ninguno abierto — ver Completados)*

## Alta prioridad (brechas directas del brief)

- [x] ~~Home sin `<h1>` real en HTML~~ — resuelto 2026-09-11: `<h1 class="sr-only">` agregado en `HomeHero` con el mismo texto que ya tenía el `alt` de la imagen. No cambia nada visual, le da a los buscadores/lectores de pantalla un encabezado real. Ver [[Decisiones y Razonamiento]].
- [x] ~~Catálogo — filtro de precio~~ — resuelto 2026-09-11: inputs de mínimo/máximo en `CatalogView`, con placeholder mostrando el rango real del catálogo.
- [x] ~~Catálogo — filtro de disponibilidad~~ — resuelto 2026-09-11: checkbox "Solo disponibles". De paso se corrigió `StorefrontProduct.stock`, que en el adaptador de Vendure estaba hardcodeado a `10` siempre (nunca se había necesitado hasta ahora) — ahora sale del `stockLevel` real de cada variante.
- [x] ~~Catálogo — paginación~~ — resuelto 2026-09-11: paginación client-side (12 productos por página) en `CatalogView`. Con el catálogo de 8 productos de hoy nunca se activa (1 sola página, controles ocultos a propósito) — el patrón queda listo para cuando el catálogo crezca, no hizo falta rehacer `storefront.ts` para paginación server-side todavía.
- [x] ~~SEO por producto~~ — resuelto 2026-09-11: `generateMetadata` real en `producto/[slug]` y `categorias/[slug]` (título, descripción, OG, canonical, todo desde datos reales de Vendure/categorías). También se le agregó `description`+canonical a `/tienda` y `/categorias`, y se reescribió la meta description del layout raíz (antes mencionaba "Vendure", ahora tiene copy real orientado a búsqueda: camas/accesorios/mascotas/Colombia). Ver [[Decisiones y Razonamiento]].
- [x] ~~`sitemap.xml` / `robots.txt`~~ — resueltos 2026-09-11: `src/app/sitemap.ts` (home, tienda, categorías, cada categoría, cada producto — carrito/checkout/cuenta quedan afuera a propósito) y `src/app/robots.ts`. Nuevo `src/lib/site-config.ts` centraliza la URL del sitio (default `https://patilandia.com.co`, el dominio ya documentado como destino — pisable con `NEXT_PUBLIC_SITE_URL`).
- [x] ~~Checkout sin estado real~~ — resuelto en la Fase 5 de la migración a Vendure (2026-09-11): `/checkout` ya tiene estado real, dirección, método de envío y confirmación de pedido reales. Ver [[Decisiones y Razonamiento]].

## Media prioridad
- [x] ~~Animaciones/microinteracciones~~ — resuelto 2026-09-11: se instaló `motion` (el sucesor de `framer-motion`) y se creó `components/ui/fade-in.tsx`, un wrapper reusable con fade + slide-up al entrar en viewport (`whileInView`, dispara una sola vez). Aplicado a las 4 secciones del home que están debajo del fold (colecciones, destacados, producto propio destacado, nuestra historia) — el hero y el strip de categorías, que están arriba del fold, se dejaron sin animar a propósito (no tiene sentido animar algo que ya es visible al cargar). Ver [[Decisiones y Razonamiento]].
- [x] ~~`/cuenta` sin lógica~~ — resuelto parcialmente 2026-09-11, a propósito no del todo: la tarjeta de Favoritos ahora es real (`AccountWishlistCard`, lee el wishlist real del store y muestra miniaturas + link a `/wishlist`). Las otras 3 (Órdenes, Direcciones, Mascotas) siguen sin lógica y ahora lo dicen explícitamente ("Próximamente") en vez de simular ser reales — **no se construyó autenticación de clientes**, porque eso está en la lista de "Correctamente diferido" más abajo (decisión consciente del brief, no algo que se deba resolver de paso). Ver [[Decisiones y Razonamiento]].
- [x] ~~Sin tests~~ — resuelto 2026-09-11: Vitest configurado (`vitest.config.mts`, scripts `npm test`/`npm run test:watch`), con 27 tests reales sobre la lógica más valiosa y menos obvia del proyecto: `lib/utils.ts`, `lib/shipping.ts` (`getShippingPreview`), y sobre todo `lib/vendure/adapters.ts` (`adaptVendureProduct` — dedupe de talla/color, conversión de precio, lectura de facets, cómputo de stock). No se agregó Playwright/e2e todavía — quedó como siguiente paso natural si se quiere ir más allá de tests unitarios.
- [x] ~~Tarjetas oscuras con gradiente violeta/azul sin tocar~~ — resuelto 2026-09-11: las 5 tarjetas (`page.tsx` x2, `cuenta/page.tsx`, `categorias/page.tsx`, `product-detail.tsx`) pasaron de gradiente a relleno plano (`var(--brand-violet)`/`var(--brand-violet-deep)`), texto blanco sin cambios — mismo patrón exacto que ya se había aplicado a los botones el 2026-09-09, no una decisión nueva. Ver [[Decisiones y Razonamiento]].

## Nuevo tras la pasada de diseño/mobile (2026-09-05)

- [x] ~~Wishlist consolidado a medias~~ — resuelto parcialmente 2026-09-11: wishlist ya tiene una presencia real dentro de `/cuenta` (ver arriba), no solo en el bottom nav. **No se tocó** `MobileBottomNav` — la pestaña de Wishlist ahí sigue existiendo tal cual, a propósito, para no meterse con la navegación principal sin que el usuario lo pida explícitamente. Si en algún momento se quiere sacar esa pestaña del bottom nav y dejar wishlist solo dentro de Cuenta, es una decisión de navegación aparte.
- [x] ~~`MenuIcon`/`CloseIcon` sin usar~~ — vuelven a estar en uso, ver 2026-09-09 abajo.

## Roadmap de funcionalidades propias de Patilandia (plugins sobre Vendure)

El roadmap original de admin (2026-09-04, diagnóstico sobre Medusa) quedó superado por la migración a Vendure — ver [[Patilandia sobre Medusa Admin — Diagnóstico y Roadmap]] (marcada histórica) y la Fase 6 en [[Decisiones y Razonamiento]]. El patrón vigente para cada funcionalidad propia de Patilandia es un plugin Vendure independiente (entidad + servicio + resolver + su porción de Dashboard), siguiendo el molde de `patilandia-admin`/`patilandia-reviews`. El orden de esta lista quedó a criterio de Claude (el usuario lo delegó explícitamente el 2026-09-11) — no es un compromiso, solo el orden elegido hasta ahora:

- [x] ~~Reseñas de producto~~ — completo 2026-09-11, primer plugin propio construido de punta a punta (backend + Dashboard de moderación + storefront + JSON-LD). Ver detalle en [[Decisiones y Razonamiento]] y en Completados abajo.
- [x] ~~Perfiles de mascotas~~ — completo 2026-09-11, segundo plugin propio. Identidad por correo sin login real (mismo patrón que el carrito), `/cuenta/mascotas` en el storefront, moderación/soporte en el Dashboard reemplazando el placeholder de la Fase 6. Ver detalle en [[Decisiones y Razonamiento]] y en Completados abajo.
- [x] ~~Wishlist como concepto de cliente real en Vendure~~ — completo 2026-09-12, tercer plugin propio. A diferencia de Mascotas, usa "local + sync perezoso" en vez de un gate de correo (decisión explícita del usuario) para no meterle fricción al corazón de un tap del catálogo. Ver detalle en [[Decisiones y Razonamiento]] y en Completados abajo.
- [x] ~~Patipuntos (fidelización)~~ — completo 2026-09-12, cuarto plugin propio. Ganar y canjear, ledger auditable, verificación de correo propia (magic link, sin password) como parte del gate de canje junto con correo verificado + primera compra. Ver detalle en [[Decisiones y Razonamiento]] y en Completados abajo.
- [x] ~~Recomendaciones de producto~~ — completo 2026-09-11, sin necesitar plugin nuevo (el catálogo real ya trae lo necesario). Reemplaza el bug real de `getFallbackRelatedProducts()` (siempre usaba mocks). Ver detalle en [[Decisiones y Razonamiento]] y en Completados abajo.
- [ ] Configurador de camitas / productos personalizables.
- [ ] Regalos.
- [ ] Suscripciones (recompras).
- [ ] Integración con WhatsApp / automatizaciones.
- [ ] Analítica propia.
- [ ] Proveedores y compras/abastecimiento (equivalente a los módulos `supplier`/`purchase-order` del roadmap viejo).

## Correctamente diferido (no es pendiente, es decisión consciente — no tocar sin razón)

- Wompi / Mercado Pago — brief los marca como fase posterior explícitamente.
- Cálculo real de envío por peso/volumen/destino/transportadora — arquitectura preparada, ver [[Shipping — Arquitectura de Envíos]].
- Autenticación de clientes.
- Dominios de producción (`patilandia.com.co`, `administrador.patilandia.com.co`, `api.patilandia.com.co`) y servidor real (VPS/GCP con PM2+nginx, al estilo Argus) — hoy todo corre en local, el despliegue es un paso futuro explícitamente pospuesto.

## Completados

- [x] **2026-09-12 — Patipuntos: contraentrega no gana puntos hasta confirmar la entrega.** El disparador de `PURCHASE`/`FIRST_PURCHASE` pasó a depender del método de pago: prepago sigue premiando en `PaymentAuthorized` (sin cambios), contraentrega espera a que el admin confirme la entrega desde el Dashboard (`OrderStateTransitionEvent → Delivered`). Se creó el método de pago `cash-on-delivery` (mismo `dummy-payment-handler` que `standard-payment`, solo un código distinto) vía `configure-checkout.ts`, y un selector real en el checkout del storefront. Hallazgo real verificado contra el servidor: `Delivered` no es alcanzable directo desde `PaymentAuthorized` — el admin tiene que liquidar el pago (`settlePayment`) primero, después crear un Fulfillment y transicionarlo a `Shipped`/`Delivered`. Verificado de punta a punta (contraentrega sin puntos hasta la entrega confirmada; prepago sigue ganando de inmediato sin duplicar si también llega a `Delivered` después). Ver [[Decisiones y Razonamiento]].
- [x] **2026-09-12 — Patipuntos: sistema de fidelización completo, cuarto plugin propio.** Plugin nuevo `patilandia-vendure/src/plugins/patilandia-loyalty` (5 entidades: `LoyaltyAccount`, `LoyaltyTransaction` como ledger append-only con `idempotencyKey` único, `LoyaltyRule` configurable desde el Dashboard sin deploy, `LoyaltySettings`, `LoyaltyVerificationToken` para un magic-link de verificación de correo propio — no la verificación nativa de Vendure, que exige contraseña real). Se gana por compra (1 pto/$500), registro (+80, editado desde 100 vía Admin durante la verificación), primera compra (+200), registrar mascota (+50), reseña aprobada (+50), cumpleaños de mascota (+100, tarea programada diaria). Se canjea con tope del 20% del subtotal **y** un gate de elegibilidad explícito pedido por el usuario (correo verificado + al menos una compra completada, en vez de una regla arbitraria de antigüedad) — diseñado como lista extensible de chequeos, no un `if` rígido. Diseñado en Plan Mode con 3 agentes de exploración + 1 de validación de arquitectura antes de escribir código. Se encontraron y corrigieron 6 bugs reales solo visibles contra el servidor y el navegador reales: dos condiciones de carrera (`CustomerService` de Vendure chocando al crear el mismo cliente nuevo en paralelo), un `DataTypeNotSupportedError` de TypeORM con columnas `string | null` sin tipo explícito, un crash de Vendure al guardar una `Order` sin `lines`/`surcharges` cargadas, un gate de "canje activo" que no contemplaba que el ledger es inmutable, y — el más importante — **un bug preexistente y grave en el checkout** (`updateCustomerName` leía un closure obsoleto y sobreescribía a `""` el correo de todo cliente nuevo en su primer pedido desde la Fase 5, nunca detectado hasta ahora). También se corrigió que la estimación de puntos ganados usaba el subtotal con IVA en vez de sin IVA. Verificado de punta a punta contra el servidor real (secuencia completa por GraphQL, incluida cancelación con reversa) y con Playwright contra Chrome del sistema (compra → puntos ganados → verificar correo real vía el mailbox de dev → segunda compra canjeando el máximo → estimación exacta contra el ledger real, 288=288). `typecheck`/`lint`/`test` (34/34)/`build` (17/17) limpios en `patilandia`, `tsc --noEmit` limpio en el server y Dashboard de Vendure (4 extensiones detectadas). Quedan preparados sin construir: campañas/multiplicadores, expiración activa, `REVIEW_WITH_PHOTO` (Reseñas no soporta fotos todavía), dashboard de métricas avanzadas. Ver [[Decisiones y Razonamiento]].
- [x] **2026-09-12 — Wishlist real: tercer plugin propio, "local + sync perezoso".** Plugin nuevo `patilandia-vendure/src/plugins/patilandia-wishlist` (entidad `WishlistItem` con FK a `Customer` y `Product`, servicio con `add`/`remove` idempotentes y un `sync` que fusiona sin borrar nunca, API de Shop pública `myWishlist`/`addToWishlist`/`removeFromWishlist`/`syncWishlist`, Admin de solo lectura + borrado de soporte, página nueva en el Dashboard). El corazón del catálogo sigue siendo 100% instantáneo y local mientras no haya un correo conocido (checkout o el email gate de Mascotas) — recién ahí sincroniza en segundo plano, decisión explícita del usuario frente a copiar el gate de Mascotas. `wishlist` en `store-provider.tsx` pasó de guardar slugs a `StorefrontProduct.id` (4 call sites actualizados). Se encontraron y corrigieron dos bugs reales de relaciones TypeORM no cargadas (invisibles a `tsc`) y un bug real de React (el efecto secundario de red vivía dentro del callback de `setWishlist`, que StrictMode invoca dos veces en dev, duplicando la mutación por click) — los tres solo aparecieron al verificar contra el servidor real y en el browser con Playwright, no con typecheck/build. Verificado de punta a punta (curl contra Vendure real + Playwright headless contra Chrome del sistema): corazón → aparece en wishlist → correo conocido vía Mascotas → siguiente corazón sincroniza una sola vez → sobrevive un reload. `typecheck`/`lint`/`test` (34/34) limpios en `patilandia`, `tsc --noEmit` limpio en el server y sin errores propios en el Dashboard de Vendure. Ver [[Decisiones y Razonamiento]].
- [x] **2026-09-11 — Recomendaciones de producto: "Relacionados" real con cross-sell, sin plugin nuevo.** `getFallbackRelatedProducts()` (siempre comparaba contra el array mock) se reemplazó por `getRelatedProducts()` en `storefront.ts`, que combina "más como este" (misma categoría/tema) con "va bien con esto" (categorías complementarias vía un mapa explícito — cama ↔ accesorios/alimentos/higiene) intercalados, filtrado por `petType` compatible. Ajuste pedido por el usuario tras ver el primer resultado: que comprar un cojín también sugiera snacks/arena, no solo más camas. Se encontró y corrigió de paso que ordenar el pool complementario solo por rating dejaba que camitas (6 productos, todos en rating 0 hoy) taparan al único snack por empate — se alterna una por categoría en su lugar. No hizo falta un plugin Vendure nuevo. Lógica extraída a `rankRelatedProducts`, función pura, con 7 tests. Verificado contra el servidor real en ambas direcciones (cama → cojín+snack, cojín → camas+snack). `typecheck`/`lint`/`test` (34/34) limpios. Ver [[Decisiones y Razonamiento]].
- [x] **2026-09-11 — Perfiles de mascotas: segundo plugin propio, identidad por correo sin login real.** Plugin nuevo `patilandia-vendure/src/plugins/patilandia-pets` (entidad `PetProfile` con FK a `Customer`, servicio con verificación de dueño por correo, API de Shop pública para gestionar "mis mascotas" y API de Admin de solo lectura/borrado de soporte bajo `Permission.ReadCustomer`/`DeleteCustomer`). Se resolvió la tensión de "esto es de un cliente pero no hay login" identificando por correo solamente — mismo nivel de confianza que ya usa el carrito, y un patrón que el propio `CustomerService.createOrUpdate()` de Vendure sanciona explícitamente en su documentación para checkouts de invitado. Dashboard: la página placeholder "Mascotas" de la Fase 6 se reemplazó por la real, moviéndose de `patilandia-admin` al nuevo plugin (misma ruta y sección de navegación). Storefront: nueva ruta `/cuenta/mascotas` (alta/edición/borrado) y la tarjeta "Mascotas" de `/cuenta` (antes "Próximamente") ahora es real con conteo real. De paso se encontró y corrigió un bug real de GraphQL: una descripción de varias líneas con comillas simples rompe el parser (`Unterminated string`) — las descripciones de una comilla no soportan saltos de línea. Verificado con la secuencia completa contra el servidor real (crear con correo nuevo → listar → actualizar → rechazo por correo incorrecto sin filtrar existencia → verla en Admin con dueño → borrar) y `typecheck`/`lint`/`test` (27/27)/`build` (15/15 páginas) limpios en `patilandia`. Ver [[Decisiones y Razonamiento]] para el detalle completo.
- [x] **2026-09-11 — Reseñas de producto: primer plugin propio de Patilandia sobre Vendure, de punta a punta.** Plugin nuevo `patilandia-vendure/src/plugins/patilandia-reviews` (entidad `ProductReview`, servicio con recálculo automático de `Product.customFields.rating/reviewCount`, API de Admin para moderar y API de Shop pública para leer/enviar). Página de moderación nueva en el Dashboard (`dashboard/index.tsx`, tabla con tabs Pendientes/Publicadas/Todas y acciones Aprobar/Ocultar/Eliminar), sumada a la sección "Patilandia" que ya existía de la Fase 6. En el storefront: `ProductReviews` (lista + formulario público, sin requerir cuenta) en la página de producto, componente `RatingStars` compartido que reemplaza el patrón viejo de 5 estrellas doradas fijas por relleno proporcional al rating real, y `StorefrontProduct.id` nuevo (necesario para pedir/enviar reseñas por producto). JSON-LD (`Product`/`Offer`/`AggregateRating`/`Review`) agregado a la página de producto, omitiendo `aggregateRating`/`review` por completo cuando el producto no tiene reseñas reales todavía, en vez de mandarle un rating de 0 a Google. Verificado con la secuencia completa contra el servidor real (enviar → pendiente → aprobar → aparece pública y en el agregado → borrar → agregado vuelve a 0/0), `npx tsc --noEmit -p tsconfig.dashboard.json`, y en `patilandia`: `npm run typecheck`/`lint`/`test` (27/27) y `npm run build` (14/14 páginas) limpios. Ver [[Decisiones y Razonamiento]] para el detalle completo, incluyendo por qué se usó el cliente GraphQL simple del Dashboard en vez del framework `DataTable` tipado.
- [x] **2026-09-11 — ESLint arreglado: migrado a flat config.** `.eslintrc.json` (formato legacy) reemplazado por `eslint.config.mjs`, usando `FlatCompat` (de `@eslint/eslintrc`, agregado como devDependency explícita) para seguir usando `next/core-web-vitals` y `next/typescript` de `eslint-config-next` (que todavía no exporta un flat config nativo en la v15.5.25 instalada). El primer intento tiró **6296 problemas** porque el flat config no excluye `.next/` automáticamente como sí lo hacía el setup legacy vía `next lint` — se agregó un bloque `ignores` explícito (`.next/**`, `node_modules/**`, `next-env.d.ts`, etc.). Con eso, el proyecto real tenía un solo warning real (`postcss.config.mjs` exportando un objeto anónimo) — se corrigió también. `npm run lint` queda en cero errores y cero warnings.
- [x] **2026-09-11 — Impuesto corregido al 19% real de Colombia.** Ver el ítem de la sección de migración más arriba y [[Decisiones y Razonamiento]] para el detalle completo (incluye un hallazgo real sobre cómo Vendure resuelve la zona de impuesto).
- [x] **2026-09-09 — Bug de raíz: `a { color: inherit }` sin `@layer` rompía el color de texto en todos los `<Link>` del sitio.** El botón del hero "no tenía letras blancas" pese a `text-white` en `buttonStyles` — verificado con `getComputedStyle`, el color real era `--ink` (oscuro). Causa: esa regla en `globals.css` estaba fuera de cualquier `@layer` de Tailwind, y las reglas sin capa le ganan a las utilidades sin importar especificidad. Fix: envolverla en `@layer base`. De paso se encontró y corrigió el mismo tipo de bug en el estado "activo" de la navegación (`site-header.tsx`, `mobile-bottom-nav.tsx`), que nunca mostraba el violeta por tener dos clases de color apiladas en vez de mutuamente excluyentes. Ver [[Decisiones y Razonamiento]].
- [x] **2026-09-09 — Tipografía Permanent Marker solo en el título de colecciones; descripciones removidas del home.** Se probó `Permanent Marker` (`@fontsource/permanent-marker`, self-hosted) como reemplazo global de `--font-display`, pero el usuario decidió volver a Baloo 2 en general y quedarse con Permanent Marker solo para "Mundos mágicos para cada personalidad" (nueva prop `titleFont` en `SectionHeading`, ver [[Decisiones y Razonamiento]]). También se sacaron los párrafos descriptivos de "Nuestras colecciones" y "Destacados" en el home (`description` ahora es opcional en `SectionHeading`).
- [x] **2026-09-09 — Home: sin tarjeta blanca en categorías, sin franja de beneficios.** `CategoryStrip` (íconos de categorías bajo el hero) perdió el contenedor blanco con borde/sombra — quedan los íconos sueltos sobre el fondo de la página. `BenefitsStrip` ("Envíos a todo el país", "Compra segura", "Textiles premium", "Atención cercana") se eliminó por completo del home y se borró el componente (`benefits-strip.tsx`), sin uso en ningún otro lado. Los mismos `benefits` de `mock-store.ts` se siguen usando en la tarjeta "Más que productos, experiencias" más abajo en la home — esa no se tocó.
- [x] **2026-09-09 — Botón del hero mobile, otro 20% más chico.** El tamaño `xs` de `buttonStyles` pasó de `h-8 px-3 text-xs` a `h-[26px] px-[10px] text-[10px]` (~80% del tamaño anterior); ícono del hero mobile ajustado a juego (`h-2.5 w-2.5`). Solo se usa en el botón del hero mobile, así que se ajustó directamente sin crear un tamaño nuevo. Desktop sin cambios ("quedó perfecto").
- [x] **2026-09-09 — Botón del hero: más chico en mobile, corrido a la derecha en desktop.** Se agregó un tamaño `xs` (`h-8 px-3 text-xs`) a `buttonStyles`/`ButtonSize` — antes `sm` era el más chico disponible. Mobile usa `xs` ahora; desktop se corrió de `px-10 lg:px-14` a `px-20 lg:px-24` (más adentro desde el borde izquierdo). Ver [[Decisiones y Razonamiento]].
- [x] **2026-09-09 — Botón del hero: chico y a la izquierda en mobile, grande y a la izquierda también en desktop.** `HomeHero` separó el CTA en dos overlays independientes (`md:hidden` / `hidden md:flex`) en vez de un solo botón con clases responsivas mezcladas, evitando el bug de doble-botón-visible por conflicto de `display` en Tailwind. Verificado con Playwright en 390/1440px y `npm run build`. Ver [[Decisiones y Razonamiento]].
- [x] **2026-09-09 — Botón del hero de vuelta dentro de la imagen, centrado; botones sin gradiente.** El CTA "Explorar la tienda" volvió a ser overlay sobre la imagen del hero (mobile y desktop), ahora centrado horizontalmente para no pisar el texto horneado en la imagen (el intento anterior, alineado a la izquierda, sí lo pisaba). Por separado: `buttonStyles` (variant `primary`) pasó de gradiente violeta a relleno plano `var(--brand-violet)`; el variant `gold` (gradiente dorado, 2 usos) se eliminó y esos 2 botones pasaron a `primary`. Verificado con Playwright en home/tienda/cuenta/categorías y `npm run build`. Ver [[Decisiones y Razonamiento]].
- [x] **2026-09-09 — Hero mobile también a `aspect-ratio` completo, botón fuera de la imagen.** Mobile dejó el recorte `object-left` (que ocultaba ~65% de la imagen) y pasó a `aspect-[1786/881]` como desktop — imagen completa, sin recortar, en cualquier ancho. El botón "Explorar la tienda" ya no vive superpuesto sobre la imagen (a 192px de alto en mobile no había margen seguro) — ahora es un bloque separado debajo, mismo color de fondo, en flujo normal. Verificado con Playwright en 390/768/1024/1280/1920px y `npm run build`. Ver [[Decisiones y Razonamiento]].
- [x] **2026-09-09 — Hero con imagen distinta por breakpoint + `aspect-ratio` para desktop.** `HomeHero` ahora usa `imagen-home.png` solo en mobile (`md:hidden`, altura fija 450px, `object-left`) e `imagen-home-destok.png` (2098×749, subida por el usuario) desde `md:` en adelante, con `aspect-[2098/749]` en la `<section>` (no en el contenedor interno) para que la imagen se muestre siempre completa sin recortar texto en ningún ancho de escritorio. Verificado con Playwright en 390/768/1024/1280/1920px y `npm run build`. Ver [[Decisiones y Razonamiento]].
- [x] **2026-09-09 — Hero del home reemplazado por `imagen-home.png`, full-bleed.** `HomeHero` ya no vive dentro de una tarjeta `max-w-7xl` con bordes redondeados — la imagen llega hasta las orillas (`object-cover object-left`, sesgada a la izquierda para no cortar el texto incrustado en la imagen en mobile). Se sacó el `<h1>`/`<p>`/tarjeta decorativa HTML (duplicaban el texto que la imagen nueva ya trae dibujado) y el botón secundario "Ver camitas propias" — queda solo "Explorar la tienda". Verificado con Playwright en 390px/1440px y `npm run build`. Genera deuda de SEO nueva (`<h1>` faltante, ver arriba). Ver [[Decisiones y Razonamiento]].
- [x] **2026-09-09 — Buscador del header funcional, navega a `/tienda?buscar=...`.** Los dos inputs de búsqueda del header (barra desktop `md:` y panel mobile) ahora son `<form>` controlados: al enviar (Enter), navegan a `/tienda?buscar=<query>` vía `router.push`. `CatalogView` lee el param `buscar` con `useSearchParams` y lo usa como valor inicial/sincronizado de su filtro de búsqueda ya existente — mismo mecanismo que ya filtraba por tamaño/mascota/categoría, sin necesitar catálogo "final" (funciona igual con los 8 productos actuales). `tienda/page.tsx` y `categorias/[slug]/page.tsx` envuelven `CatalogView` en `<Suspense>` (requisito de Next.js para `useSearchParams` en build estático). Verificado con `npm run build` (mock fallback) sin warnings, y con Playwright en mobile/desktop: buscar "snack" y "camita galaxy" filtran correctamente. Ver [[Decisiones y Razonamiento]].
- [x] **2026-09-09 — Cuenta/Carrito fuera del header mobile, lupa directa.** Por debajo de `md`, `SiteHeader` reemplaza los íconos de Cuenta/Carrito (redundantes con el bottom nav) por un botón de lupa que abre un panel de búsqueda visual (sin `onChange`). El menú hamburguesa quedó solo con navegación. Desde `md:` sigue igual (íconos + barra de búsqueda de escritorio). Verificado con Playwright en 390px y 1024px, sin errores de consola. Ver [[Decisiones y Razonamiento]].
- [x] **2026-09-09 — Menú hamburguesa mobile restaurado.** `SiteHeader` recupera el botón hamburguesa (`lg:hidden`) con drawer de navegación. Wishlist se queda fuera del header, como en el rediseño del 2026-09-05. Ver [[Decisiones y Razonamiento]].
- [x] **2026-09-05 — Logo real + favicon completo.** Arreglado el bug de aspect-ratio del logo (declaraba 220×80, el archivo real era 1254×1254 cuadrado — causaba que el header saltara de tamaño al cargar); reemplazado por `logo-rectangular.png` (lockup horizontal). Agregado `src/app/apple-icon.png` (180×180) — sin él, iOS/Android no mostraban ícono. `icon.png` regenerado más grande y con menos padding.
- [x] **2026-09-05 — Categorías con imágenes reales, "Snacks" eliminada.** 7 categorías con foto propia cada una (antes ícono SVG plano). `snack-crunch-pollo` reasignado a "Alimentos". Grids de 8→7 columnas donde correspondía.
- [x] **2026-09-05 — Mobile-first en cards de producto y colección.** Todos los grids de `ProductCard`/`CollectionCard` pasan a 2 columnas desde el primer breakpoint (antes 1 columna hasta 640px). `ProductCard` responsive real: paddings, tamaños de texto, badge, botón wishlist y botón "Agregar"/"Agregar al carrito" (se acorta en mobile) todos con `sm:` breakpoint.
- [x] **2026-09-05 — Header simplificado.** Sin menú hamburguesa (bottom nav mobile ya cubre navegación), logo centrado en mobile vía grid `[1fr_auto_1fr]`, sin ícono de wishlist en el header (queda solo en bottom nav mobile).
- [x] **2026-09-04 — Instalar y conectar Medusa.** Backend Medusa 2.20.1 corriendo en local (`patilandia-backend`, repo hermano), Postgres en Docker (puerto 5433, evitando conflicto con una instancia nativa de Postgres ya presente en la máquina), usuario admin creado, publishable key generada y linkeada al Default Sales Channel. Ver [[Entorno de Desarrollo Local]].
- [x] **2026-09-04 — Migrar los 8 productos Patilandia a Medusa.** Catálogo demo genérico de Medusa (camisetas) borrado (soft-delete); los 8 productos reales creados vía workflows oficiales con `metadata` fiel al esquema que `adaptMedusaProduct` ya esperaba — cero regresión visual. Ver [[Decisiones y Razonamiento]].
- [x] **2026-09-04 — `HomePage` y `WishlistPage` ya no rompen la capa de adaptación.** `HomePage` es ahora `async` y usa `getHomepageProducts()`/`getHomepageCollections()` de `lib/storefront.ts`; `WishlistRoute` hace `await getStorefrontProducts()` y pasa los productos como prop a `WishlistPage`. Las 9 rutas pasan consistentemente por `lib/storefront.ts`.
- [x] **2026-09-04 — Adaptador Medusa mejorado: galería de imágenes real.** `adaptMedusaProduct` ahora lee `record.images` (array real de Medusa) para `galleryImages`, con fallback a `[thumbnail]` — antes solo repetía el thumbnail.
- [x] **2026-09-04 — `.env.local` del storefront apuntado a Medusa real**, con `NEXT_PUBLIC_MEDUSA_BACKEND_URL`, `NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY` reales. Verificado con `npm run typecheck` limpio y las 9 rutas respondiendo 200 con datos reales de Medusa (precio, imágenes, título todos correctos).

*(Antes de esta sesión, el código ya traía resuelto: homepage, catálogo con filtros parciales, product detail con galería/variantes, carrito y wishlist persistentes, identidad visual completa, capa de adaptación Medusa — ver [[Contexto Patilandia]].)*

Tags: #claude #pendientes #tareas
