NEXT_PUBLIC_VENDURE_SHOP_API_URL=http://localhost:3000/shop-api
NEXT_PUBLIC_STORE_CURRENCY=COP
# Usado en metadataBase, sitemap.xml y robots.txt. Sin definir, cae en https://patilandia.com.co
# (el dominio de producción ya decidido, ver la bóveda) — no hace falta en local a menos que
# quieras probar las URLs absolutas apuntando a localhost.
NEXT_PUBLIC_SITE_URL=
